<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jost:wght@300&display=swap" rel="stylesheet"> 
    <link rel="stylesheet" href="/css/comentarios.css">
    <title>Mensajes</title>
</head>
<body>
    <section class="container-mensajes">
        <h1 class="h1-mensajes">Listado de mensajes</h1>
        <% for(let i = comentarios.length - 1; i >= 0; i--) { %>
            <div class="container-individual">
                <% if(i != comentarios.length - 1) {%>
                    <hr class="hr-mensajes">
                <% } %>
                <h3>De: <%= comentarios[i].usuario %></h3>
                <p><%= comentarios[i].comentario %></p>
            </div>
        <% } %>
    </section>
    <section class="container-botones">
        <div class="boton">
            <div><a href="/">Volver</a></div>
            <div><a href="/comentarios">Actualizar</a></div>
        </div>
    </section>
</body>
</html>